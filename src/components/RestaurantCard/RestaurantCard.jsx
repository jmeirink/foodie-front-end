import { Link } from 'react-router-dom'

const RestaurantCard = (props) => {
  return ( 
    <>
      <h1>test rest card</h1>
      {/* <Link to={`/spell/${props.spell.index}`}>
        {props.spell.name}
      </Link>
      <br/> */}
    </>
  )
}

export default RestaurantCard;